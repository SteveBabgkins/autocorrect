-- The server part of the hook for fucking text correction you can also change it I don't give a shit

local PLUGIN = PLUGIN;


function PLUGIN:Initialize()
    Clockwork.kernel:PrintLog(LOGTYPE_GENERIC, "Auto Text Correction plugin has been initialized.");
end
